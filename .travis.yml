sudo: false
language: ruby
rvm:
  - 2.2.0
  - 2.3.0
  - 2.4.0
  - jruby-9.1.7.0
services:
  - rabbitmq
  - redis
before_install:
  - gem install bundler --version 1.15.1
script: "bundle exec rspec . --tag ~ssl"
